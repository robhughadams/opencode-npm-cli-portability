{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-npm-cli-portability",
  "description": "npm-first CLI-only portability fork for opencode",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "packages/opencode"
  ],
  "scripts": {
    "build": "npm run build --workspace opencode",
    "test": "npm run test --workspace opencode"
  }
}
